# Python

## Setup

Install dependencies using Poetry:

```shell
poetry install
```

## Running the tests

To run tests once, execute:

```shell
poetry run pytest
```

To run tests in watch mode, execute:

```shell
poetry run ptw
```
